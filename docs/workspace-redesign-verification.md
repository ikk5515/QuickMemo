# Memo and wiki workspace redesign verification

Authoritative specification: user attachment `pasted-text-1.txt`, 72 numbered requirements, received 2026-09-05. The first candidate `3f5aabdbfaed32a236b65de99b7e2c6667c0aeca` failed CI; the follow-up fixes and verification are recorded below. The earlier e9d1814 release is a baseline, not completion of this specification. Local results and production acceptance are recorded separately below.

## Implementation boundaries

- Preserve encrypted note/folder data and existing auth, rules, sanitizer, recovery and security headers.
- One owner has one public wiki root with an atomic unique custom slug; folders/notes extend that tree. Explicit publication grants stay separate from edit rights.
- One live Markdown editing surface, independent in-memory document/undo/dirty/save/scroll state, no plaintext/key browser persistence.
- All open wiki documents remain independently addressable, closable and collapsible with vertical title strips and measured motion.
- UI dimensions and collapse preferences are non-sensitive metadata. Inspect server preference support before choosing persistence.
- Legacy public snapshots must remain readable until an owner explicitly migrates them. Never delete original notes or silently broaden publication scope.
- Production validation must use explicitly labeled synthetic notes/folders/wiki data. Existing personal data is not a test fixture.

## Requirement evidence ledger

`Local verified` describes the named local evidence only; it does not claim production completion. `Pending` and `in progress` identify required remaining work. A green test is evidence only for the behavior it covers.

| # | Requirement | State | Evidence / remaining check |
|---|---|---|---|
| 1 | 기본 구현 방향 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 2 | 첨부 이미지 사용 기준 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 3 | 제 PC를 이용한 직접 분석 허용 | Verified | Opened installed Obsidian 1.13.7 and reference browser. Created only labeled comparison notes; inspected live Markdown, checklist and split-pane/menu behavior. |
| 4 | 개발자 도구와 화면 측정 허용 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 5 | 메모 편집 방식 전면 변경 | Local verified | One CodeMirror 6 Live Editor on Memo and owner Wiki; no mode selector. Existing encryption, conflict, history and image controllers reused. Legacy HTML stays intact; an explicit Markdown copy opens in a new panel. Editor/legacy unit suites and real new-note save/reload pass. |
| 6 | 단일 Live Editing Editor | Local verified | One CodeMirror 6 Live Editor on Memo and owner Wiki; no mode selector. Existing encryption, conflict, history and image controllers reused. Legacy HTML stays intact; an explicit Markdown copy opens in a new panel. Editor/legacy unit suites and real new-note save/reload pass. |
| 7 | Editor 기술 검토 | Local verified | One CodeMirror 6 Live Editor on Memo and owner Wiki; no mode selector. Existing encryption, conflict, history and image controllers reused. Legacy HTML stays intact; an explicit Markdown copy opens in a new panel. Editor/legacy unit suites and real new-note save/reload pass. |
| 8 | Editor 기본 동작 | Local verified within stated scope | Firefox and Chromium native input + encrypted save/reload: 4 browser cases pass; 101 related unit cases pass. Korean/Japanese final DOM is consumed before composition completion, save and preview restoration. Real Android hardware and OS candidate-window interaction remain untested. |
| 9 | 메모와 위키의 편집 경험 통일 | Local verified | One CodeMirror 6 Live Editor on Memo and owner Wiki; no mode selector. Existing encryption, conflict, history and image controllers reused. Legacy HTML stays intact; an explicit Markdown copy opens in a new panel. Editor/legacy unit suites and real new-note save/reload pass. |
| 10 | 문서 상단 UI | Local verified | One CodeMirror 6 Live Editor on Memo and owner Wiki; no mode selector. Existing encryption, conflict, history and image controllers reused. Legacy HTML stays intact; an explicit Markdown copy opens in a new panel. Editor/legacy unit suites and real new-note save/reload pass. |
| 11 | 문서 `…` 메뉴 | Local verified | One CodeMirror 6 Live Editor on Memo and owner Wiki; no mode selector. Existing encryption, conflict, history and image controllers reused. Legacy HTML stays intact; an explicit Markdown copy opens in a new panel. Editor/legacy unit suites and real new-note save/reload pass. |
| 12 | Obsidian 전용 기능 제거 | Local verified | One CodeMirror 6 Live Editor on Memo and owner Wiki; no mode selector. Existing encryption, conflict, history and image controllers reused. Legacy HTML stays intact; an explicit Markdown copy opens in a new panel. Editor/legacy unit suites and real new-note save/reload pass. |
| 13 | 여러 문서를 동시에 열 수 있는 구조 | Local verified | One CodeMirror 6 Live Editor on Memo and owner Wiki; no mode selector. Existing encryption, conflict, history and image controllers reused. Legacy HTML stays intact; an explicit Markdown copy opens in a new panel. Editor/legacy unit suites and real new-note save/reload pass. |
| 14 | Wiki 다중 문서 구조 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 15 | 공간 부족 시 Panel Collapse | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 16 | 문서 열기·닫기·전환 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 17 | 모든 관련 동작에 자연스러운 Animation 적용 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 18 | Animation 성능 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 19 | `내 메모` Sidebar Resize | Local verified | Shared pointer/keyboard divider, bounded widths, collapse/reopen, UID-scoped server metadata with CAS and anonymous local fallback. Actual desktop drag, server acknowledgement and refresh recovery pass; no plaintext/key stored in preferences. |
| 20 | Wiki Sidebar Resize | Local verified | Shared pointer/keyboard divider, bounded widths, collapse/reopen, UID-scoped server metadata with CAS and anonymous local fallback. Actual desktop drag, server acknowledgement and refresh recovery pass; no plaintext/key stored in preferences. |
| 21 | Sidebar 열기/닫기 | Local verified | Shared pointer/keyboard divider, bounded widths, collapse/reopen, UID-scoped server metadata with CAS and anonymous local fallback. Actual desktop drag, server acknowledgement and refresh recovery pass; no plaintext/key stored in preferences. |
| 22 | Sidebar 상태 저장 | Local verified | Shared pointer/keyboard divider, bounded widths, collapse/reopen, UID-scoped server metadata with CAS and anonymous local fallback. Actual desktop drag, server acknowledgement and refresh recovery pass; no plaintext/key stored in preferences. |
| 23 | UI 전체 크기 축소 및 정보 밀도 개선 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 24 | Wiki 좌측 메뉴 디자인 전면 개선 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 25 | Public Wiki URL 구조 전면 변경 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 26 | 사용자별 고정 Wiki Root | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 27 | Wiki ID는 전체 서비스에서 중복 불가 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 28 | Wiki ID 규칙 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 29 | 내부 ID와 Public Wiki ID 분리 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 30 | Wiki 데이터 구조 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 31 | Folder 추가 동작 | Local verified | Saved descendants extend the same root, explicit individual grants preserve folder grants, and excluded siblings stay private. Auto-publication 17 cases cover debounce, no-op signatures, CAS, cancellation and legacy consent. Real folder/child creation and anonymous read flow pass. Dataview stale widgets, composition races and sequential-close races were also corrected. |
| 32 | Note 추가 동작 | Local verified | Saved descendants extend the same root, explicit individual grants preserve folder grants, and excluded siblings stay private. Auto-publication 17 cases cover debounce, no-op signatures, CAS, cancellation and legacy consent. Real folder/child creation and anonymous read flow pass. Dataview stale widgets, composition races and sequential-close races were also corrected. |
| 33 | 하위 Folder 및 Note도 동일 | Local verified | Saved descendants extend the same root, explicit individual grants preserve folder grants, and excluded siblings stay private. Auto-publication 17 cases cover debounce, no-op signatures, CAS, cancellation and legacy consent. Real folder/child creation and anonymous read flow pass. Dataview stale widgets, composition races and sequential-close races were also corrected. |
| 34 | Public Wiki 방문자에게 동일한 Tree 표시 | Local verified | Saved descendants extend the same root, explicit individual grants preserve folder grants, and excluded siblings stay private. Auto-publication 17 cases cover debounce, no-op signatures, CAS, cancellation and legacy consent. Real folder/child creation and anonymous read flow pass. Dataview stale widgets, composition races and sequential-close races were also corrected. |
| 35 | Wiki Root는 변경되지 않음 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 36 | Note 선택 시 URL 정책 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 37 | Wiki ID 변경 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 38 | Wiki ID Race Condition 방지 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 39 | Wiki Slug Server-side Validation | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 40 | 기존 Random Wiki URL Migration | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 41 | 자동 저장 | Local verified | Independent memory-only drafts/history/scroll, guarded debounced autosave, stable editor subtrees and serialized close actions. Four-editor browser text/scroll/undo and resize persistence pass. Render-isolation tests show unrelated editors are not repainted by typing or sidebar resize. |
| 42 | 문서 전환 중 데이터 유실 방지 | Local verified | Independent memory-only drafts/history/scroll, guarded debounced autosave, stable editor subtrees and serialized close actions. Four-editor browser text/scroll/undo and resize persistence pass. Render-isolation tests show unrelated editors are not repainted by typing or sidebar resize. |
| 43 | 문서별 Scroll Position 유지 | Local verified | Independent memory-only drafts/history/scroll, guarded debounced autosave, stable editor subtrees and serialized close actions. Four-editor browser text/scroll/undo and resize persistence pass. Render-isolation tests show unrelated editors are not repainted by typing or sidebar resize. |
| 44 | Public Wiki 오류 화면 간소화 | Local verified | Missing/revoked/invalid public links render only the requested failure heading. Public visitors receive no My Wiki/login/edit controls. Route tests and real anonymous browser/API denial pass. |
| 45 | Public Wiki 방문자 `내 위키` 제거 | Local verified | Missing/revoked/invalid public links render only the requested failure heading. Public visitors receive no My Wiki/login/edit controls. Route tests and real anonymous browser/API denial pass. |
| 46 | 공개 권한과 편집 권한 완전 분리 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 47 | State Management 구조 개선 | Local verified | Independent memory-only drafts/history/scroll, guarded debounced autosave, stable editor subtrees and serialized close actions. Four-editor browser text/scroll/undo and resize persistence pass. Render-isolation tests show unrelated editors are not repainted by typing or sidebar resize. |
| 48 | React Rendering 최적화 | Local verified | Independent memory-only drafts/history/scroll, guarded debounced autosave, stable editor subtrees and serialized close actions. Four-editor browser text/scroll/undo and resize persistence pass. Render-isolation tests show unrelated editors are not repainted by typing or sidebar resize. |
| 49 | 전체 성능 최적화 | Local verified | Saved descendants extend the same root, explicit individual grants preserve folder grants, and excluded siblings stay private. Auto-publication 17 cases cover debounce, no-op signatures, CAS, cancellation and legacy consent. Real folder/child creation and anonymous read flow pass. Dataview stale widgets, composition races and sequential-close races were also corrected. |
| 50 | 보안 전면 점검 | Local verified | 87 Rules tests and 112 API-emulator tests pass; sanitizer, attachment signatures/schemes, active-user authorization and headers retained. Functions/gitignore/billing guards pass; audit reports 0 vulnerabilities. Public production headers and denial checks are pending deployment. |
| 51 | Markdown Rendering 보안 | Local verified | 87 Rules tests and 112 API-emulator tests pass; sanitizer, attachment signatures/schemes, active-user authorization and headers retained. Functions/gitignore/billing guards pass; audit reports 0 vulnerabilities. Public production headers and denial checks are pending deployment. |
| 52 | IDOR 집중 검증 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 53 | Public Wiki 권한 처리 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 54 | Security Header | Local verified | 87 Rules tests and 112 API-emulator tests pass; sanitizer, attachment signatures/schemes, active-user authorization and headers retained. Functions/gitignore/billing guards pass; audit reports 0 vulnerabilities. Public production headers and denial checks are pending deployment. |
| 55 | 반응형 동작 | Local browser verified | The 60 core combinations across Chrome, Firefox, Chromium and WebKit at 320–1440px passed after affected-case reruns. Independent native input, source, recovery, conflict, Library promotion and pane regressions also pass. Native Edge and Android/iOS hardware are not claimed. |
| 56 | 접근성 | Local browser verified | The 60 core combinations across Chrome, Firefox, Chromium and WebKit at 320–1440px passed after affected-case reruns. Independent native input, source, recovery, conflict, Library promotion and pane regressions also pass. Native Edge and Android/iOS hardware are not claimed. |
| 57 | 주요 브라우저 호환성 | Local browser verified | The 60 core combinations across Chrome, Firefox, Chromium and WebKit at 320–1440px passed after affected-case reruns. Independent native input, source, recovery, conflict, Library promotion and pane regressions also pass. Native Edge and Android/iOS hardware are not claimed. |
| 58 | Regression Test | Local browser verified | The 60 core combinations across Chrome, Firefox, Chromium and WebKit at 320–1440px passed after affected-case reruns. Independent native input, source, recovery, conflict, Library promotion and pane regressions also pass. Native Edge and Android/iOS hardware are not claimed. |
| 59 | Multi-document 집중 테스트 | Local browser verified | The 60 core combinations across Chrome, Firefox, Chromium and WebKit at 320–1440px passed after affected-case reruns. Independent native input, source, recovery, conflict, Library promotion and pane regressions also pass. Native Edge and Android/iOS hardware are not claimed. |
| 60 | Wiki URL 집중 테스트 | Local verified | 112 real API-emulator tests plus backend/service/route cases cover atomic global slug claims, one owner root, tampering/IDOR, legacy adoption, source revocation and preference UID isolation. Browser root creation, readable links, rename (old 404) and unpublish (404) pass. Production checks remain separate. |
| 61 | 원하는 Wiki의 최종 동작 예시 | Local verified | Saved descendants extend the same root, explicit individual grants preserve folder grants, and excluded siblings stay private. Auto-publication 17 cases cover debounce, no-op signatures, CAS, cancellation and legacy consent. Real folder/child creation and anonymous read flow pass. Dataview stale widgets, composition races and sequential-close races were also corrected. |
| 62 | 레퍼런스와 QuickMemo 반복 비교 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 63 | 레퍼런스 판단 우선순위 | Local verified | Reference measurements and real A/B/C/D interactions informed 36px strips, compact rows, persistent independent panels and 180ms transitions. Reader/reducer/render-isolation tests and Chromium desktop browser flows pass. Settled public geometry is checked separately from a transition screenshot. |
| 64 | PC 사용 범위 | Verified | User unlocked macOS and signed in/unlocked production Chrome. Native Obsidian comparison completed with synthetic notes. Four labeled QA notes were saved inside a dedicated QA folder; existing personal notes were not edited. No saved credentials or unrelated personal files accessed. |
| 65 | 작업 중 발견한 추가 문제 | Local verified | Saved descendants extend the same root, explicit individual grants preserve folder grants, and excluded siblings stay private. Auto-publication 17 cases cover debounce, no-op signatures, CAS, cancellation and legacy consent. Real folder/child creation and anonymous read flow pass. Dataview stale widgets, composition races and sequential-close races were also corrected. |
| 66 | 코드 품질 | Local release gates passed | 337 unit files / 3,419 tests pass; 62 emulator-only cases are separately covered. Lint/typecheck, guards and audit pass; production build passed after the final CSS adjustment. The final CSS build and affected browser reruns pass. No new dependency, secret, index, Rule or bulk migration. |
| 67 | 자동 테스트 | Local release gates passed | 337 unit files / 3,419 tests pass; 62 emulator-only cases are separately covered. Lint/typecheck, guards and audit pass; production build passed after the final CSS adjustment. The final CSS build and affected browser reruns pass. No new dependency, secret, index, Rule or bulk migration. |
| 68 | 배포 전 확인 | Local release gates passed | 337 unit files / 3,419 tests pass; 62 emulator-only cases are separately covered. Lint/typecheck, guards and audit pass; production build passed after the final CSS adjustment. The final CSS build and affected browser reruns pass. No new dependency, secret, index, Rule or bulk migration. |
| 69 | 실제 운영 환경까지 배포 | Pending release and production evidence | Implementation and local gates do not prove deployment. Require a successful current-master CI, the same-SHA Vercel workflow, alias smoke and synthetic authenticated production checks before completion. |
| 70 | 배포 후 실제 운영 환경 검증 | Pending release and production evidence | Implementation and local gates do not prove deployment. Require a successful current-master CI, the same-SHA Vercel workflow, alias smoke and synthetic authenticated production checks before completion. |
| 71 | 최종 완료 기준 | Pending release and production evidence | Implementation and local gates do not prove deployment. Require a successful current-master CI, the same-SHA Vercel workflow, alias smoke and synthetic authenticated production checks before completion. |
| 72 | 최종 결과 보고 | Pending release and production evidence | Implementation and local gates do not prove deployment. Require a successful current-master CI, the same-SHA Vercel workflow, alias smoke and synthetic authenticated production checks before completion. |

## Architectural decisions and current measurements

- Retain CodeMirror 6: it preserves Markdown source and supports existing syntax, search, image and sanitizer extensions. An editor replacement would duplicate already secured persistence and recovery. Document sessions are memory-only and bounded; key/authority changes clear them.
- Owner Wiki runs through `VaultPage(surface="wiki")`, avoiding a second editor/save implementation. Public Wiki receives only the staged authorized read projection and never mounts a writable editor.
- Custom slugs are ASCII lowercase, 3–40 characters, with a reserved-name denylist. Internal publication and source IDs are not used in the public browser URL. Renaming invalidates the old address; another owner cannot claim its tombstone.
- Publication remains an explicit grant. Folder grants include descendants; individual-note grants expose the note at the public root. Automatic publishing cannot add a new grant. Original encrypted records remain unchanged.
- Sidebar preference API accepts only width/collapsed metadata, selects its document from authenticated UID, checks same origin/App Check/active user and bounds, and uses CAS/rate limiting. Local fallback stores no note, key or plaintext body.
- Unit benchmark on this machine (not a production latency claim): 120 notes, full baseline 183.98ms; metadata refresh 0.78ms with 0 RSA/0 AES; one body update 1.76ms with 0 RSA/1 AES. A 5,000-note metadata refresh eliminated 5,000 RSA and 10,000 AES calls. Logs `/tmp/qm-redesign-unit.log`.
- The first integration run exposed 14 failures in three tests using the former WikiPage/mode/panel behavior. These were resolved while retaining permission and source-fidelity assertions; the final full-suite result below supersedes that run.

## Earlier verification checkpoints (historical)

- Full unit checkpoint before the final browser fixes: 336 files and 3,409 tests passed; 62 emulator-only tests skipped here. API emulator integration separately passed 112 tests and Rules passed 87 tests. Full lint/typecheck/security guards passed, dependency audit has zero vulnerabilities. `/tmp/qm-redesign-final-summary.md`.
- The intermediate production build passed. The final build below includes the later browser-discovered fixes.
- Chromium desktop browser round 2: five tests passed, including actual encrypted folder publication, raster publication, anonymous source isolation, saved descendant auto-publication, explicit note grant expansion, slug rename and revocation. One failing desktop density assertion exposed CodeMirror's stronger line-number style. `/tmp/qm-redesign-browser-round2.log`.
- New public root flow keeps one `/wiki/{slug}` for later descendants and explicit grants; old renamed and revoked URLs return actual API 404 and discard visible panels. No original ciphertext changes are needed for public snapshots.
- Release review found the new standalone preference endpoint would exceed the Hobby function budget. Preferences now dispatch through authenticated `/api/vault-integrity?resource=workspace-preferences` to a private helper, retaining the dedicated request marker and all authority checks. The function-budget guard rejects a thirteenth deployed endpoint.
- Native comparison confirmed compact icon navigation, live headings/lists/checklists/wiki links and the document options menu. Korean/Japanese text rendered correctly; native text injection is not proof of OS IME composition behavior. Browser composition tests are recorded separately.
- Follow-up Chromium desktop regressions passed after correcting stale Dataview widget data, the right-divider transition race, hidden line-number gutters and the integrity request counter. Independent document edits/scroll/undo, folder-root automatic publication and preferences persisted after refresh. `/tmp/qm-redesign-browser-fixed-regressions.log`.
- Four-document open: 873.2ms including UI work, p95 frame interval 16.7ms, no observed long tasks. Sidebar resize with four mounted editors and server preference acknowledgement: 669.4ms, p95 frame interval 9.8ms, no observed long tasks. Synthetic local fixtures; not a production network-latency claim. `/tmp/qm-redesign-evidence/desktop/`.
- Graph benchmark passed all four Chromium/WebKit desktop/mobile cases with 5,000 nodes and 10,000 edges. First display 932–1,005ms, filter p95 18.3–27ms, observed animation 59.8–120.2fps. `/tmp/qm-redesign-graph-performance.log`.
- Firefox native composition exposed a real input-loss race: preview decoration dispatch ran before CodeMirror consumed the final IME DOM mutation. The final shared lifecycle, native-event probe, encrypted memo reload and affected browser matrix cases pass. CI includes a Firefox lane so this regression cannot be silently skipped.


## Deployment boundary update

- Vercel Hobby supports 12 API functions for this Vite deployment. Preferences now dispatch only through `POST /api/vault-integrity?resource=workspace-preferences`; the underscore helper is not an additional function. Runtime API count is 12. The Functions guard and a 12-pass/13-fail fixture regression enforce this limit.
- Preferences retain a separate marker, same-origin, active-user and App Check checks, a 2KiB body bound, UID-derived document lookup and metadata-only projection. Missing, duplicate and unknown resources cannot invoke this branch as an integrity action.
- No new environment variable, credential, Firestore index/Rule or bulk migration is required. New owner/slug/preferences documents use the existing server REST identity and remain denied through client Firestore access. Legacy migration is an explicit user action.
- The detailed current URL/publication contract is in `docs/wiki-publishing.md`. This architectural update is not a production deployment claim.

## First candidate local release checkpoint

- Unit: 337 files / 3,419 tests passed; 62 emulator-only tests excluded from this command and separately covered. `npx vitest run --maxWorkers=4` uses the same full suite as `npm test`; passing a second maxWorkers flag through npm was rejected before tests ran and was corrected.
- API emulator: 112 passed; Firestore/Storage Rules: 87 passed. No backend/Rule changes followed these gates.
- Lint and typecheck passed on the final production code. Functions guard: 12/12; sensitive-file and billing guards pass; dependency audit: 0 vulnerabilities.
- Final production build passed, including chunk verification and the Library extension artifact. Logs: `/tmp/qm-redesign-release-*.log`, with `-final` suffix for final lint/typecheck/build.
- Core browser matrix: 50/60 initially passed. The 10 failed cases were fixed and all passed in the targeted recheck: Firefox synthetic composition bracketing, Chrome favicon requests, mobile menu width, and WebKit divider stabilization. No security assertions were removed.
- The targeted recheck additionally exercised the complete desktop-1280 regression selection: 26 passed / 6 project-specific skips; two old test assumptions (removed source-mode selector, cross-frame pane measurements) were fixed. Both passed in `/tmp/qm-redesign-browser-last-two.log` (17.3s).
- Public screenshots now wait for visible transitions and assert that the active document occupies the remaining width. Actual final Chrome and tablet layouts were inspected; an earlier clipped image was captured during its transition, not a settled layout. Screenshots and measurements: `/tmp/qm-redesign-evidence/`.
- Production release and the 39-step production acceptance remain pending. These local results do not attest to the currently deployed baseline.

## First CI findings and follow-up

- CI `33963627067` for `3f5aabd` failed and did not deploy. Its full unit, Rules, API, security, performance and build gates passed. Responsive and WebKit browser lanes exposed the following boundary cases.
- At 768px with a reserved scrollbar, the 52px ribbon, 180px left panel and 250px right panel left only 271px for the editor. The inspector minimum is now 230px and both width calculations reserve the touch ribbon. The editor's 280px requirement is retained, with additional 761–1024px scrollbar regressions. Focus explicitly scrolls a partly clipped inspector tab fully into view.
- Playwright resolves `ControlOrMeta` from the test host, while CodeMirror resolves its modifier from the browser. Linux CI emulating an iPhone sent Ctrl-End instead of Meta-End. The shared test helper now sends real keyboard commands using the browser's Mac/iOS detection; Markdown and undo assertions remain intact.
- A sidebar preference acknowledgement is separate from encrypted workspace autosave. Preference tests now wait for the relevant layout save/load before hard reloads. In the app, the first trusted input records the current layout, and only an actual layout change can conflict with a late remote load. Merely focusing or using the independent Wiki sidebar no longer counts as a layout change. Real local edits and revision conflicts retain their protection; the tracker clears at access-scope changes and explicit resolution.
- The initially reported new-note title failure occurred before link activation. A later WebKit trace captured the first editor's delayed autofocus moving focus from the title field to the Markdown body during title input. The following body replacement removed that misplaced text, leaving the default title. External text-field focus is now respected during editor autofocus; regression checks preserve both deliberate title input and ordinary initial editor focus. Creation tests assert the saved input and active-tab titles immediately.
- Phone preference tests wait for the responsive drawer's closed state after restoring the phone viewport. WebKit can acknowledge a viewport change before the media-query subscriber updates React; the previous helper sometimes read the old desktop state and skipped its open click. No arbitrary delay or repeated click was added.
- A later single-browser trace proved an ambiguous commit response: the revision-4 ciphertext was present on the server even though the commit returned an error. Workspace persistence now confirms only an exact owner/revision/wrapped-key/ciphertext match from a non-cached server read after a failed attempt. Receipts are bounded to five within that call, discarded afterward, and never persisted. Ordinary successful saves add no read; genuine conflicts and failed confirmation keep the original error. The service has 28 passing tests and an independent security review found no blocker.
- Follow-up release gates and production acceptance are recorded separately after the new candidate completes CI.

## Follow-up local release checkpoint

- Full unit suite: 337 files / 3,452 tests passed, with 62 emulator-only cases excluded from this command. Workspace commit recovery has 28 tests and the CodeMirror editor has 54. `/tmp/qm-ci-fix6-unit.log`.
- Full lint and production build, including TypeScript and chunk verification, passed. Functions, sensitive-file and billing guards passed; audit found zero vulnerabilities. `/tmp/qm-ci-fix6-lint.log`, `/tmp/qm-ci-fix6-build.log`, `/tmp/qm-ci-fix3-audit.log`.
- Firestore/Storage Rules were rerun: 87 passed. The preceding candidate's 112 real API-emulator cases passed locally and in CI; backend/Rules code is unchanged by these follow-up fixes. `/tmp/qm-ci-fix3-rules.log`.
- Inspector keyboard, width, overflow and encrypted-resize cases passed across seven Firefox/Chromium/WebKit desktop/tablet/phone configurations. That run exposed two unrelated mobile preference failures, subsequently traced to responsive-test timing and first-editor autofocus. `/tmp/qm-ci-fix3-browser.log`.
- Mobile preference checks passed four repetitions after waiting for the actual responsive state. The final production-code checkpoint then passed all eight combined encrypted-vault and four-document preference cases on Chromium 768/1024 and WebKit 320/390. These retain immediate title entry, exact Markdown, undo/composition, autosave, sidebar persistence, reload and overflow assertions. `/tmp/qm-ci-fix4-mobile.log`, `/tmp/qm-ci-fix6-browser.log`.
- Independent reviews found no outstanding issue in exact-ciphertext recovery, autofocus behavior or the revised browser assertions. No dependency, security-header, Rule, environment or release-workflow change was introduced by this follow-up.
- Exact-commit CI/deployment and authenticated production acceptance still require separate evidence.

## Second CI test contract correction

- CI `33965881303` for `62c6725` reported one unit failure (3,451 passed): a Dataview test required the entire `EditorState` object to retain its identity while awaiting live results. The diff showed CodeMirror's ordinary autocomplete completion changing Pending to Inactive after its typing delay, with serialized document/selection unchanged.
- The test now preserves exact document and history identities, equal selection, the same `EditorView` and block DOM, and zero document-change callbacks. It also verifies undo and redo after live results update. The runtime implementation is unchanged. Independent review confirmed the autocomplete effect in the installed CodeMirror source and no reduction of the document-preservation contract.

## Third CI browser measurement correction

- CI `33966270452` for `0f66b43` passed all 3,452 unit tests. The Chromium desktop contrast check read a detached autocomplete row after CodeMirror replaced its list, yielding empty computed styles. It now resolves the currently visible connected row and copies its styles in one synchronous browser callback.
- Only DOM readiness is polled. The first valid measurement must parse successfully and meet the unchanged 4.5:1 minimum; low contrast is never retried. The parser handles both RGB and CSS `color(srgb ...)` with their respective channel scales and alpha composition.
- The targeted Chromium desktop 1440px test passed twice, measuring 12.1129:1 each time. Independent review found no assertion weakening. `/tmp/qm-ci-color-targeted.log`. Runtime code is unchanged by this correction; new exact-commit CI and production acceptance remain pending.

## Canonical public document boundary

- Preparing production acceptance uncovered a real route discrepancy: explicit target validation covered legacy `/wiki/public/:id` links but not `/wiki/:slug`. A missing canonical page left the empty reader visible, and an unknown `note` parameter could fall back to the first public document. This did not authorize private content, but violated the requested error and navigation behavior.
- Both routes now require a unique readable published target. Empty, unavailable, asset and ambiguous targets, including repeated parameters, show only `위키를 열 수 없습니다`. A valid legacy public entry ID redirects with history replacement to its readable page path. An explicit page retains precedence over an older note parameter.
- Thirteen canonical route regressions were added; 12 failed before the correction. All 26 page tests and 13 public-data hook tests pass. The full suite now passes 3,465 tests in 337 files; 62 emulator-only cases remain separately covered. Full lint, typecheck and production build pass. `/tmp/qm-public-canonical-*.log`, `/tmp/qm-canonical-full-unit.log`, `/tmp/qm-canonical-lint-final.log`, `/tmp/qm-canonical-build.log`.
- The API-backed browser flow now checks both missing paths and rejected original note IDs, then obtains a valid public entry ID from the returned manifest to test readable URL conversion. Public entry IDs and encrypted source IDs remain separate namespaces. The full root flow passed on Chromium desktop 1440px and WebKit mobile 320px, including saved descendants, grant expansion, rename and unpublish (2/2, no retries). `/tmp/qm-canonical-root-browser-verified.log`.
- To investigate previous CI retry passes, WebKit desktop sidebar persistence ran four times and the mobile private Wiki flow ran four times, with four additional unauthenticated deep-link checks. All passed without retries or timeout changes. The longest observed workspace save wait was 835.4ms and note preparation 397ms; related API/Firestore writes returned 200. The old CI delay was not reproduced, so its cause remains unconfirmed. `/tmp/qm-webkit-repeat-{prefs,private}-20260905*`.

## Mobile publication test readiness

- The publication test previously treated an explorer absent during route loading as a closed drawer, then waited for the ribbon's File button. An open mobile drawer makes that ribbon inaccessible. The helper now waits for the always-mounted expanded-state control and completion of initial workspace loading before deciding whether one open click is needed; it does not wait for autosave or extend timeouts.
- A delayed-mount WebKit fixture reproduced the old helper timeout. Delayed-open and restored-open cases now require zero clicks, and delayed-closed requires one. The full WebKit 320px publication flow passed three times without retries (40.9s). `/tmp/qm-publish-explorer-readiness-synthetic-20260905.json`, `/tmp/qm-publish-explorer-repeat-20260905.log`. Runtime code is unchanged; the original CI screen was not retained, so this demonstrates the helper defect rather than claiming a complete diagnosis of that CI instance.

## 81b166f 운영 QA 후속 패치 — 로컬 검증

- 운영 1512×771 화면에서 Wiki 레이아웃의 가용 높이는 673px인데 긴 폴더 트리가 암시적 grid 행을 1766px로 늘려 문서와 편집기 스크롤 영역까지 화면 밖으로 확장되는 문제를 확인했습니다. 긴 C 문서만의 문제로 단정하지 않습니다. 실제 암호화 메모 60개와 긴 본문을 사용하는 로컬 회귀에서도 수정 전 stack 높이 1850px로 실패했습니다. Desktop grid 행을 `minmax(0, 1fr)`로 제한하고 sidebar의 `min-height: 0`을 지정했으며, 좁은 화면의 본문 아래 목차 흐름은 유지했습니다.
- 수정 후 Chromium/WebKit에서 stack·sidebar가 가용 높이 673px를 지키고 긴 트리와 본문은 각 내부 영역에서 스크롤했습니다. 높이/owner strip 브라우저 회귀 4/4, 긴 트리에서 실제 C 세로 제목을 눌러 다시 여는 보강 회귀 2/2가 재시도 없이 통과했습니다. 네 문서 DOM·본문·C의 scrollTop 500 보존과 1024/390px 전환도 확인했습니다. 이 결과는 strip 자체의 JavaScript 버그나 운영 클릭 실패의 원인을 확정하지 않습니다. `/tmp/qm-wiki-height-long-tree-before-20260905.log`, `/tmp/qm-wiki-height-strip-after-20260905.log`, `/tmp/qm-wiki-long-tree-strip-final-20260905.log`.
- 새 이미지 삽입 source가 제목 또는 문단과 붙을 수 있어, 생성하는 이미지 참조의 앞뒤와 이미지 사이에 빈 줄을 두어 독립 Markdown 블록으로 만듭니다. 기존 저장 본문을 일괄 재작성하지 않습니다. 실제 CodeMirror 파일 추가 경로에서 이미지 렌더링, 기존 제목·본문 보존, 단일 undo 및 정확한 source 문자열 rollback을 검증했고 독립 리뷰에서 blocker를 발견하지 않았습니다.
- 현재 패치 전체 gate: 단위 338개 파일 / 3,468 tests 통과, 4개 파일 / 62 tests 제외, 실패 0; Firestore/Storage Rules 87/87, API emulator 112/112 통과. Lint, typecheck, production build, Functions·sensitive-file·billing guards 통과; dependency audit 취약점 0건입니다. `/tmp/qm-post-production-fixes-{unit,rules,api,lint,typecheck,build}-20260905.log`.
- 공개 범위 밖의 참조 이미지는 동의 없이 자동 포함하지 않습니다. 운영에서 새 QA 하위 폴더와 G 메모를 저장한 뒤, 이미 열린 익명 페이지의 revision이 5→7로 바뀌고 같은 Wiki root에서 G의 실제 본문을 읽는 것까지 확인했습니다. 이미지가 0개였던 결과는 별도 이미지 폴더가 공개 범위 밖이기 때문이며 자동 반영 실패로 간주하지 않습니다.
- 개별 선택 UI가 모든 asset을 제외하던 누락을 보완했습니다. 소유권·참여자·활성 상태와 실제 PNG/JPEG/WebP 형식을 검증한 이미지를 명시적으로 고를 수 있습니다. 공개 크기 제한을 넘는 본문은 JSON/base64 decode 전에 거절하고, 선택 후보 검사는 공개 설정이 열려 있을 때만 실행합니다. 이미지 이름을 최종 확인 목록에 표시하며, 선택 변경은 동의를 초기화합니다. 서버 권한·grant 구조·기존 원본은 변경하지 않습니다.
- 실제 PNG 두 개를 업로드한 Chromium/WebKit 브라우저 회귀 2/2가 통과했습니다. 메모와 첫 이미지만 선택했을 때 확인 전 게시 쓰기가 없고, 게시 후 폴더 0개·선택 항목 2개·이미지 naturalWidth 1, 다른 이미지와 원본 폴더의 미노출, 원본 메타데이터 보존을 확인했습니다. `/tmp/qm-wiki-individual-image-e2e-20260905.log`. 독립 코드 리뷰에 추가 blocker는 없었습니다.
- 추가 UI까지 반영한 전체 단위 checkpoint는 339개 파일 / 3,480개 통과, 62개 제외였습니다. 이후 새로 추가했던 소스 문자열 비교 1개를 제외하고 기존 Vault UI 계약 파일을 재검증했습니다(제품 코드는 동일). 동작을 검증하는 이미지 선택·동의·공개 경계 단위/E2E는 유지합니다. 최종 lint, typecheck를 포함한 production build, 보안 guards, audit 0건도 통과했습니다. `/tmp/qm-final-image-choice-{unit,lint,build}-20260905.log`, `/tmp/qm-final-vault-ui-contract-20260905.log`. Rules/API 제품 코드는 앞선 87/112 통과 이후 변경하지 않았습니다.
- 이 기록 시점의 후속 패치는 **미커밋·미배포** 상태입니다. 위 결과는 로컬 패치 검증이며, 새 SHA의 CI·배포 및 운영 재확인이나 전체 39개 운영 수용기준 완료를 뜻하지 않습니다.

## CI owner inventory pagination correction

- Candidate `ec2604d` passed CI quality gates (3,479 unit tests, 87 Rules and 112 API cases), Firefox, responsive Chromium and Secure Share, but desktop Chromium failed in the newly added owner-strip test. The UI activation and saved-state checks passed; the subsequent server-revision comparison threw because C was absent from the test inventory. This candidate was not deployed.
- `listEmulatorCollection` requested `pageSize=300` once and ignored `nextPageToken`; the fixture then filtered that incomplete collection by owner. In a real 301-document emulator reproduction, the server returned only 150 documents plus a token. The helper reported no current-owner note even though a direct document read found it at revision 4. The corrected helper returned all 301 and the owner note. `/tmp/qm-emulator-pagination-before-20260905.log`.
- The test helper now follows encoded continuation tokens, including after short or empty pages, and rejects malformed/repeated tokens, failed responses and more than 100 pages instead of returning partial data. Nine focused regressions pass. Product code, production queries, permissions and security assertions are unchanged.
- The browser test additionally requires four distinct created IDs and all four server records before editing C. With the 301-document inventory retained, Chromium 1440/1280 each passed twice without retries; C revision increases once, D stays unchanged and all document DOMs remain mounted. `/tmp/qm-owner-strip-pagination-desktop-20260905.log`. Independent review found no blocker. This fixture defect does not establish the cause of the earlier production strip-click observation.
- The complete local unit suite passes 3,488 tests in 340 files, with 62 emulator-only cases separately covered. Full lint and typecheck/build pass. API integration was rerun and all 112 tests passed. `/tmp/qm-pagination-final-{unit,lint,build}-20260905.log`, `/tmp/qm-pagination-fix-api-20260905.log`. The new exact-SHA CI and deployment remain pending.

## Remaining mobile explorer readiness consumers

- The completed `ec2604d` WebKit lane passed with five retries. Two first-attempt failures used the same premature `isVisible()` explorer branch already corrected in the publication test. The root-sync and private-Wiki helpers now wait for the mounted toggle and initialized workspace before reading `aria-expanded`; they click once only when the drawer is actually closed. Product code, timeouts and behavioral assertions are unchanged.
- Extracted real helpers passed twelve delayed-open, delayed-closed, restored-open and lock-to-restore cases; the old branch reproduced its timeout. WebKit 390px then passed root publication, private Wiki and authentication deep-link flows (3/3, no retries, 49.1s). Full lint and script syntax checks passed. `/tmp/qm-two-explorer-readiness-20260905.json`, `/tmp/qm-two-explorer-webkit390-20260905.log`, `/tmp/qm-mobile-readiness-final-lint-20260905.log`.
- The other three WebKit retry passes involved note-creation readiness. The new long-document case timed out at B's title before creating the long C body or importing the tree, so it is not evidence of the height regression recurring. Similar disabled/detached title symptoms exist in baseline runs. Without the CI trace or request timing, their detailed cause remains unconfirmed; they are not dismissed as test-only failures.

## Profile subscription stability and date-independent verification

- Candidate `127dbdf` passed quality (3,488 unit tests, 87 Rules and 112 API cases), Chromium, Firefox and Secure Share, but failed two WebKit390 cases; production workflow `33973817967` was skipped. The built-in tools retry crossed midnight in Asia/Seoul: the test captured the previous date before login while the template correctly used the new date at creation. That test now fixes only `Date`, leaving animation, network and autosave timers running. Six Chromium/WebKit date-boundary cases and the complete WebKit390 tools flow passed without retries.
- A separate local calendar unit failure occurred because September 6 is the final trailing cell of the fixed August grid. Its right-arrow test assumed today's cell always had a next cell. It now verifies one initial tab stop and focuses a known interior date before checking right-arrow navigation; all five calendar tests pass. Calendar runtime behavior is unchanged.
- The earlier first-attempt save-state disappearance and private-Wiki creation failure were not reproduced in four fresh WebKit390 runs (4/4). Their exact CI cause remains unconfirmed. Failure-only diagnostics now report bounded counts, enumerated UI states, approved endpoint names and HTTP status, excluding note text, IDs, URL queries, credentials and raw errors. All existing assertions and timeouts remain intact; five privacy regression cases pass.
- Independently, the note and folder effects depended on the complete profile object although their profile input is only the UID. They now retain subscriptions across unrelated same-owner metadata updates. UID/private-key changes, effective owner scope, decryption session and explicit integrity retry remain dependencies, and all access-revocation cleanup is unchanged. An actual browser regression holds a dirty save while the profile display name changes twice, verifies the same editor DOM and exact draft, then releases and confirms the save. This does not establish the cause of the original CI failure.
- The complete local suite passes 3,493 tests in 341 files, with 62 emulator-only cases separately covered. Typecheck/build, full lint, Functions/sensitive-file/billing guards and audit (zero vulnerabilities) pass. `/tmp/qm-subscription-final-unit-green-20260906.log`, `/tmp/qm-subscription-final-build-20260906.log`, `/tmp/qm-subscription-final-lint-20260906.log`. The production release and authenticated acceptance require fresh same-SHA evidence.
- With 600 synthetic documents from 30 previous owners retained in the emulator, WebKit390 passed publication, root synchronization, preferences, private Wiki and unauthenticated deep links (5/5, no retries, 1.4 minutes). The final profile-refresh regression passed Chromium1280 and WebKit390 (2/2, no retries, 19.8 seconds), retaining the exact draft and the same editor DOM and confirming exactly one new server revision. `/tmp/qm-wiki-create-loaded-20260906.log`, `/tmp/qm-profile-refresh-final-20260906.log`. These are local results, not a claim that the earlier CI failure was reproduced or resolved.
- Final local Rules and API emulator reruns also passed: 87/87 and 112/112 respectively. `/tmp/qm-subscription-final-rules-20260906.log`, `/tmp/qm-subscription-final-api-20260906.log`. No backend, Rules, dependency, environment or deployment-workflow behavior was changed by this follow-up.

## Individually published root images referenced by nested notes

- Production `f3282ab` passed CI and deployment, and authenticated checks confirmed four retained editors, 673px bounded panel height, direct collapsed-strip activation, scroll restoration and independent saving. A separate anonymous check found a real image regression: a selected PNG existed in the public manifest and its API returned 200, but the nested note displayed `[비공개 첨부]`. The publication wrote a bare canonical root filename, which the public projection interpreted relative to the note's folder.
- New publication wikilinks now explicitly start at `/`. Existing publications resolve normal relative references first; only an unresolved wikilink with a complete filename can fall back to one exact root path in the current public manifest. The projection and asset reader share this rule. Ambiguous relative/root candidates, unselected assets, partial names and encoded external targets remain blocked. No private source, server authorization, MIME checks, dependency or security header changed.
- Ten new projection/parser/asset-reader cases reproduced seven failures before the fix and all pass after it. The focused suite passes 53 tests. The full suite passes 3,503 tests in 342 files, Rules 87 and API 112; lint, typecheck/build, security guards and audit pass. Chromium/WebKit browser coverage passes four cases without retries (40.0s): both an individually granted root note and a folder-granted nested note reference one separately granted root PNG, while a private sibling image stays excluded, the original metadata stays unchanged and the image survives reload. New CI, deployment and real public image/revocation evidence are required before closing this regression.

## Root link identity across rendering and knowledge indexing

- Candidate `e04e8b4` was not deployed: its Firefox public-folder flow failed twice at `wiki-publish.vault.mjs:101`, retaining one panel after a document-link click. The exact test failed locally too. The knowledge parser removed leading `/`, while the renderer retained it and the reader matched targets exactly. This was a product regression, not a retry-only or browser timing failure.
- The knowledge parser now preserves root notation, and path resolution handles a leading `/` before ordinary relative Markdown paths. Valid relative paths keep their previous behavior. A verified legacy public root-file fallback canonicalizes only the derived display/index Markdown, preserving the original API body, alias, fragment and encoding. Reader click, preview, graph and backlinks therefore share the same target without a reader-specific exception.
- Four actual Reader regressions and three core index regressions failed before the fix and pass afterward. The related knowledge/wiki suite passes 298 tests; the complete suite passes 3,513 in 344 files with 62 emulator-only cases excluded. Three-engine browser verification passes seven tests without retries (1.3 minutes), including the original Firefox publication/revocation flow, Chromium/WebKit publication and both root/nested image cases; the two image cases remain intentionally excluded in Firefox. Full lint, typecheck/build and independent review pass. A new same-SHA CI/deployment and actual public-image check remain required.
- Final Rules and API emulator reruns pass 87 and 112 cases, alongside Functions/sensitive-file/billing guards and audit with zero vulnerabilities. Evidence: `/tmp/qm-root-links-final-{unit,rules,api,lint,build}-20260906.log` and `/tmp/qm-public-root-links-browser-20260906.log`.

## Compact document transitions and close contents

- The requirement-17 audit found that compact Wiki reactivation hid the previous document immediately and showed the next without intermediate motion. An actual Chromium frame-sampling regression failed against `dd93ddb` with zero intermediate opacity frames. This is separate from the completed production root-link/PNG fix, whose existing revision-10 publication rendered its selected PNG without republishing.
- Compact neighbors now retain their full viewport width at a small offset on the appropriate side of the active document. Transform and opacity animate on activation; visibility changes only after the outgoing transition. Existing DOM, editor state and scroll positions remain mounted, while inactive/exiting articles become inert immediately. Reduced motion removes the transition and visibility delay.
- Closing now preserves the document's prior expanded/collapsed presentation while it fades. An expanded desktop document no longer switches abruptly to a strip before disappearing; compact close retains its width during the exit. Authorization removal still removes unavailable notes immediately, and save guards and close cleanup are unchanged.
- Chromium, Firefox and WebKit each passed the real-frame regression without retries (22.3s). It checks compact reactivation/close, desktop entry/close and neighboring width movement, original DOM/scroll retention, immediate inertness, 390/1440/320px resizing, no overflow, and zero animations with reduced motion. Public API responses are local synthetic fixtures; this is UI behavior coverage, not a production or backend authorization check. `/tmp/qm-mobile-motion-before-20260906.log`, `/tmp/qm-panel-motion-full-20260906.log`.
- The full local unit suite passes 3,514 tests in 344 files, with 62 emulator-only cases excluded; lint, typecheck/build, security guards, dependency audit (zero vulnerabilities), Rules87 and API112 pass. Fresh same-SHA CI/deployment and production motion verification remain required. `/tmp/qm-motion-final-{unit,lint,build,rules,api,functions,gitignore,billing,audit}-20260906.log`.

## Markdown checklist alignment and graph dialog focus

- The deployed public PNG screenshot exposed a real Markdown checklist alignment defect: the checkbox inherited the application's 42px minimum input height. A local public-reader fixture reproduced a 13.3875px downward center offset. The Markdown checkbox now resets only its own width/height/min-height/padding and aligns to the first text line; no document content, renderer data, or global input styles change. Three engines across 1440/768/320px and light/dark themes passed 72 task-row measurements, with maximum center offset 0.0084px, preserving checked/disabled state, wrapped and nested tasks, ordinary lists and zero overflow. Evidence: `/tmp/qm-checkbox-before-20260906.log`, `/tmp/qm-checkbox-final-20260906/summary.json`.
- A fresh anonymous production probe followed F's graph node to A and A's backlink back to F, retaining both documents. It then reproduced failure to restore opener focus after closing the expanded graph. The parent tried to focus a background button before the native modal released its inert background. Closing the native dialog before the existing close callback fixes button and Escape dismissal without changing navigation, permissions or graph state. The original Chromium regression failed; the combined checkbox/focus suite passes six tests across Chromium, Firefox and WebKit without retries (9.7s). Focus is checked at 1440/320px for both dismissal methods. Evidence: `/tmp/qm-graph-focus-before-20260906.log`, `/tmp/qm-graph-focus-after-20260906.log`.
- These are local synthetic public-reader regressions. Independent review found no blocker. Full unit verification passes 3,514 tests with 62 emulator-only cases excluded; lint, typecheck/build, security guards and audit (zero vulnerabilities) pass. Actual OS input, owner preference persistence, live publication revocation and authenticated second-account checks remain separate and unverified. A new same-SHA CI/deployment and public verification are required for this follow-up.

## Expired SMTP-test recovery snapshot race

- The first full API gate after the UI changes passed111/112 and returned500 instead of200 in `accounts a finalizer-versus-recovery race exactly once`. The unchanged isolated case passed, and one unchanged full rerun passed112/112. The sanitized original log does not establish its exact cause; all three outcomes are preserved in `/tmp/qm-detail-final-api-20260906.log`, `/tmp/qm-detail-api-race-isolated-20260906.log` and `/tmp/qm-detail-final-api-rerun-20260906.log`.
- Independent review found a concrete pre-existing gap: expired-test recovery built its quota writes outside the optimistic-commit retry handler. If another finalizer consumed the reservation between settings and quota reads, `EmailTestQuotaSnapshotConflict` escaped as500. Deterministic handler-level tests reproduced that path with mocked external dependencies, without SMTP/network/production access.
- Recovery now reloads settings only for that specific snapshot conflict and retries only when both document versions exist and differ, within the existing five-attempt limit. An unchanged/missing-version/missing-document snapshot fails closed with503; exhausted changing snapshots produce409. Existing optimistic write preconditions, atomic accounting and unrelated storage-error handling remain unchanged. Nine new regressions cover the concurrent finalizer, uncontended recovery, commit-time conflict, four unavailable/underflow cases, bounded retries and unrelated failure; the related41-test unit group passes. `/tmp/qm-email-recovery-unit-red-20260906.log`, `/tmp/qm-email-recovery-unit-final-20260906.log`.

- Final post-fix local gates pass: 3,523 unit tests in345 files (62 emulator-only cases excluded), Rules87, API112, lint, typecheck/build, Functions/sensitive-file/billing guards and audit0. The unchanged UI source retains the six passing three-engine regressions above. `/tmp/qm-detail-release-{unit,rules,api,lint,build,functions,gitignore,billing,audit}-20260906.log`. Production remains3cbd4ef until the next same-SHA CI/production release is confirmed.

## Workspace acknowledgement lifetime after an access-scope cleanup

- Candidate `bccffaad82fe8c544d4c5da36de706c07aa43fb1` was not deployed: CI `33981469579` failed. Chromium1280's existing cross-context folder-draft test preserved and saved its unrelated note, but its final workspace-layout assertion saw `conflict` instead of `saved` on both attempts. WebKit also failed the long-document fixture (59 of 60 notes on the first attempt; new-note preparation on retry). Two other WebKit cases passed on retry. The unchanged folder-draft and long-document tests each passed one local reproduction; those passes do not erase the CI failures.
- Read-only diagnosis found an independent completion-order defect: a workspace write already accepted by Firestore could return after folder/ACL cleanup advanced the save generation. Discarding that old layout was correct, but discarding its successful revision receipt left the same session's next write using its own previous revision. This is distinct from a legitimate collision between different layouts in two contexts. The original CI failure's causal relationship to this defect remains unconfirmed.
- A single-context browser regression now controls this exact order. A loopback-only module wrapper calls the real workspace service and Firestore transaction, then delays one successful return for the synthetic fixture UID. The real server returns200 and advances revision3 to4 before a loopback folder tombstone triggers access cleanup. The test observes the removed folder/tab and retained authorized dirty draft before releasing the acknowledgement. Original code then saved the note but entered a workspace CAS conflict; the corrected code reaches saved and preserves the exact Markdown after reload. The wrapper changes only completion timing, remains unarmed for every other call, and adds no production route or storage. This is a controlled ordering regression, not proof of natural incidence or of the original CI root cause. `/tmp/qm-bccffaa-workspace-scope-ack-module-red-20260906.log` and its retained trace contain the failing order and numeric-only evidence.
- `useWorkspaceSaveSession` accepts only a valid, non-regressing numeric revision from the currently active UID/private-key session. A folder/ACL cleanup keeps that session but still invalidates all old plaintext/layout continuations. Key/UID changes, lock, unmount and StrictMode effect cleanup invalidate the opaque token. Autosave and exit flush acknowledge the number before checking the old access/save generation; stale serialized state, pending state and UI are never restored. Queued flush work and asynchronous conflict/retry callbacks additionally require the same active session. Existing service CAS, encryption, permission checks and manual cross-context conflict choices are unchanged.
- The16 new hook cases and28 existing workspace-service cases pass (44/44). The corrected controlled-order regression and unchanged original folder-draft test pass2/2 without retries (39.3s), including note saved, workspace saved and reload source equality. `/tmp/qm-bccffaa-workspace-save-session-unit-final-20260906.log`, `/tmp/qm-bccffaa-workspace-scope-ack-green-20260906.log`. The initial attempt to delay the HTTP Commit response blocked the same SDK's listener delivery and failed fixture preparation; that separate result is not treated as a product regression. Full release gates and a new exact-SHA CI/deployment remain required.

- Final local product gates after the session guards pass: 3,539 unit tests in346 files,62 emulator-only cases excluded, full lint/typecheck/build, Functions/sensitive-file/billing guards and audit0. `/tmp/qm-workspace-ack-final-{unit,lint,typecheck,build,functions,gitignore,billing,audit}-20260906.log`. The browser regression additionally checks that the post-cleanup server revision advances beyond the delayed receipt, removed folder/tab references remain absent both after acknowledgement and after reload, and runtime diagnostics remain clean. Rules/API and the strengthened final browser assertions are recorded separately by the release owner; no new deployment is implied by these local gates.

- A later combined browser run passed the folder-draft test's body preservation, saved-layout and reload checks but failed its retained `expectCleanRuntime` check on an initial workspace Commit403. The trace showed a revision2 write with an update-time precondition and a Rules L861 evaluation failure. Competition between the two fixture contexts' initial workspace writes is a plausible explanation, not a proven cause. The original failure remains recorded. The test now waits for the first context's persisted layout before opening the second, then waits for the editing context's initial selection layout to be saved before starting the existing folder/dirty-draft race. It neither permits the console error nor removes any original assertions or extends timeouts.
- The long-document and owner-strip fixtures additionally wait for a new active note ID before entering a title/body, verify the exact saved source and four independent IDs, and (for the long sidebar) verify all60 server records before checking navigation. Independent review found no weakened assertion: layout dimensions,60-note navigation, retained editors, scroll positions, source/revision checks and runtime diagnostics remain required. These test readiness changes do not establish the root cause of the earlier CI failures.

- Final strengthened local browser checks passed: Chromium/WebKit long-document and strip cases4/4, followed by the prepared folder-draft and strengthened acknowledgement regression2/2 without retries (38.1s). The latter records server revision3→4→5, one delayed success, final `saved`, clean runtime and no revoked folder/tab before or after reload. Rules87 also pass. `/tmp/qm-workspace-ack-final-order-browser-20260906.log`, `/tmp/qm-workspace-ack-final-order-evidence-20260906.json`, `/tmp/qm-workspace-ack-final-rules-20260906.log`.
- The first final API run passed111/112; a concurrent duplicate client-assigned create returned409 (`revision_conflict`) on one request instead of the expected200. The unchanged isolated case passed1/1 with15 other cases excluded (3.88s). The first API output is retained, but its emulator debug log was replaced by the isolated emulator startup and is unavailable for exact contention diagnosis. The existing bounded retry and exact note/claim/history validation have not been relaxed or changed. An unchanged full API rerun is recorded separately; the earlier failure is not erased or attributed to the client workspace change. `/tmp/qm-workspace-ack-final-api-20260906.log`, `/tmp/qm-workspace-ack-api-create-isolated-20260906.log`.

- The unchanged full API rerun passes112/112 in84.93s: `/tmp/qm-workspace-ack-final-api-rerun-20260906.log`. Independent backend review found the existing retry bound, exact encrypted payload/history checks and atomic creation preconditions sound; no backend code or API expectation was changed. The earlier concurrent-create409 remains a recorded intermittent failure with unconfirmed low-level contention cause, not a claimed repaired defect. All final local gates are now green; CI and production are still separate release gates.

## 2026-09-06 follow-up: Memo tab motion and exact reading-position restoration

Sections 17–18 previously had Wiki motion evidence but lacked equivalent Memo tab behavior. An encrypted loopback owner probe on `07fb5d9` confirmed that closing middle B removed it at the first animation frame and jumped C by 190px; reopening and active-document changes had no geometry/opacity animation. The original regression failed its requirement for intermediate C positions.

`MemoTabStrip` now uses a pre-commit structural snapshot and 170ms transform/opacity animation for entry, close, surviving-tab movement, and active-document changes. Logical close/save/IME/pinned/creation guards retain their immediate behavior. Only an escaped, inert, aria-hidden title survives briefly as an exit visual; no editor, interactive control, duplicate ID, or event handler is cloned. The same authorized plaintext boundary synchronously removes every exit and animation. UID/key/ACL scope changes, unmount, and reduced-motion changes also cancel timers and visuals. Typing and metadata/save changes do not trigger geometry measurements; keyboard/pointer/composition input stops the active-editor transition. Close focus is restored in both motion preferences.

The broader probe also reproduced an existing Memo scroll defect on original `07fb5d9`: reopening a long document changed scrollTop 236→473. CodeMirror's public `scrollSnapshot()` fixes the remount anchor, but its height map can still be stale during immediate reduced-motion navigation. A separate DOM measurement proved a remaining 13px change in the same document line's viewport Y; merely flushing the height map also moved the old view by 13px. `captureMarkdownEditorScroll` therefore reads the existing visible line's position, settles pending layout through public CM APIs, compensates that line's measured movement, and captures the official scroll snapshot. It runs only when an authorized editor is disposed, with bounded transient DOM references; hidden/detached/unmeasurable/update-exception cases retain the safe fallback and continue destruction. The persisted-in-memory snapshot contains coordinates only, within the existing 40-document/4,000,000-character UID/key-scoped session store. Exact-source mismatch or failed editor-state restoration discards the matching scroll snapshot as well. Existing ACL and generation cleanup is preserved.

Final browser regression: Chromium, Firefox, and WebKit × normal/reduced motion, **6 passed in 1.1 minutes**, no retries. Normal transitions recorded 12–21 intermediate C positions and 11–21 opacity states. The exit title's baseline error was at most 0.5px. Scroll remained 236→236 in Chromium/Firefox normal, 235→235 in WebKit normal, and 223→223 in all reduced-motion cases. The same document anchor's viewport Y changed **0px in all six cases**. Exact Markdown source, dirty switching, ASCII keyboard Undo/Redo, acknowledged save, rapid reopen, 320px overflow, left/current/final close, and cleanup all passed.

Earlier Firefox automation `fill`/`insertText` Undo behavior reproduced on the original source. The emitted `input.type.compose` lacked a preceding `compose.start` and was merged by CM history with the previous event. It is not counted as actual OS IME evidence and the composition-history implementation was not changed. The final ordinary-input regression uses real ASCII key events. Korean/Japanese OS input remains a separate owner acceptance item.

An earlier WebKit emulator read lost its transport and then retried with the identical loopback method/URL/body and HTTP 200. The new scenario records that exact recovered read separately; unrecovered reads, HTTP/auth errors, unrelated console errors, and runtime failures remain hard failures. The final six-case run had **zero failed/recovered reads, classified console errors, and HTTP errors**. Shared security assertions and runtime helpers were not weakened. Two existing source-structure matchers were updated only for the extracted tab component and the added plaintext-boundary dependency; the latter gained an explicit synchronous motion-clear assertion.

Evidence: `/tmp/qm-memo-motion-red-07fb5d9-20260906.log`, `/tmp/qm-memo-history-baseline-20260906.log`, `/tmp/qm-memo-history-modified-20260906.log`, `/tmp/qm-memo-scroll-geometry-20260906/`, `/tmp/qm-memo-scroll-flush-geometry-20260906/`, and final `/tmp/qm-memo-motion-anchor-final-20260906.log` plus its sibling artifact directory. Full local unit results: **3,561 passed / 62 separate-emulator skips, 348 passing files**. Rules: **87 passed**. Lint/typecheck/build/functions/gitignore/billing guards and audit (0 vulnerabilities) passed. Final API integration: **112 passed in 84.89s**, with no rerun. Same-SHA CI/Production remain separate release gates until their results are recorded.

The previously released `07fb5d9` passed CI 33983273563 and Production 33984043178; alias initial asset `/assets/index-BCagLJN9.js` matched the deployment build. Its read-only production QA verified existing PNG, checklist alignment, graph/backlink navigation, graph-dialog focus, TOC scrolling, distinct canvas F/A labels, and settled layout, without owner writes. This is release/public evidence for that SHA, not proof of the new Memo changes or actual owner acceptance. Actual OS Korean/Japanese input, latest owner sidebar hard-reload persistence, QA unpublication, and a real second authenticated account remain pending; the user Chrome tab is untouched while awaiting the requested input observation.
